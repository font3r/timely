@baseAddress = http://localhost:5000
@jobId = {{job.response.body.id}}

### Get jobs
GET {{baseAddress}}/api/v1/jobs/{{jobId}}

### Get jobs
GET {{baseAddress}}/api/v1/jobs

### Create job
# @name job
POST {{baseAddress}}/api/v1/jobs
Content-Type: application/json

{
    "slug": "test-example-job-1",
    "description": "test job description",
    "schedule": {
        "frequency": "once"
    }
}